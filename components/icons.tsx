import type { LucideIcon } from "lucide-react"
import {
  Mail,
  MessageSquare,
} from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  Mail,
  WhatsApp: MessageSquare,
  Designali: Mail, // Using Mail as a temporary replacement, you can change this to your preferred icon
} 